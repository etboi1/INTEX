<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Participant</title>

    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background: #F9F5EA;
            margin: 0;
            padding: 0;
        }

        .navbar {
            width: 100%;
            display: flex;
            justify-content: space-between;
            padding: 18px 40px;
            background: #F9F5EA;
            border-bottom: 2px solid #FFD8D1;
            box-sizing: border-box;
        }

        .navbar a {
            color: #333;
            font-weight: 500;
            text-decoration: none;
            margin-left: 20px;
        }

        .brand {
            font-family: 'DM Serif Display', serif;
            font-size: 26px;
            color: #333;
        }

        .container {
            width: 90%;
            max-width: 700px;
            margin: 40px auto;
            background: white;
            padding: 30px;
            border-radius: 14px;
            border: 2px solid #FFD8D1;
        }

        h1 {
            font-family: 'DM Serif Display', serif;
            text-align: center;
            color: #333;
            margin-top: 0;
        }

        .form-group {
            margin-bottom: 18px;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 6px;
            color: #333;
        }

        input, select {
            width: 100%;
            padding: 10px;
            border: 2px solid #FFD8D1;
            border-radius: 10px;
            font-size: 16px;
            background: #FFF9F6;
        }

        .submit-btn {
            width: 100%;
            background: #FFD8D1;
            border: 2px solid #FFC4BA;
            padding: 12px;
            border-radius: 10px;
            font-weight: 600;
            color: #333;
            cursor: pointer;
            font-size: 16px;
            margin-top: 20px;
        }

        .submit-btn:hover {
            background: #FFEEE9;
        }

        .error-box {
            background: #FFE5E2;
            color: #B20000;
            border: 2px solid #FFB8B1;
            padding: 14px 18px;
            border-radius: 10px;
            font-weight: 500;
            margin-bottom: 20px;
            text-align: center;
        }
    </style>
</head>
<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <div class="brand">Ella Rises</div>
        <div>
            <a href="/">Home</a>
            <a href="/users">Users</a>
            <a href="/logout">Log Out</a>
        </div>
    </div>

    <div class="container">
        <h1>Add Participant</h1>

        <% if (error_message) { %>
        <div class="error-box">
            <%= error_message %>
        </div>
        <% } %>

        <form action="/addPart" method="POST">

            <div class="form-group">
                <label>Email</label>
                <input type="email" name="part_email" required>
            </div>

            <div class="form-group">
                <label>First Name</label>
                <input type="text" name="part_first_name" required>
            </div>

            <div class="form-group">
                <label>Last Name</label>
                <input type="text" name="part_last_name" required>
            </div>

            <div class="form-group">
                <label>Birth Date</label>
                <input type="date" name="part_dob">
            </div>

            <div class="form-group">
                <label>Role (p for participant, a for admin)</label>
                <select name="part_role" required>
                    <option value="p">Participant</option>
                    <option value="a">Admin</option>
                </select>
            </div>

            <div class="form-group">
                <label>Phone Number</label>
                <input type="number" name="part_phone">
            </div>

            <div class="form-group">
                <label>City</label>
                <input type="text" name="part_city">
            </div>

            <div class="form-group">
                <label>State</label>
                <input type="text" maxlength="2" name="part_state">
            </div>

            <div class="form-group">
                <label>Zip</label>
                <input type="number" name="part_zip">
            </div>

            <div class="form-group">
                <label>School or Employer</label>
                <input type="text" name="part_school_or_employer">
            </div>

            <div class="form-group">
                <label>Total Donations</label>
                <input type="number" step="0.01" name="total_donations">
            </div>

            <button type="submit" class="submit-btn">Add Participant</button>
        </form>
    </div>

</body>
</html>