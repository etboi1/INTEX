<!-- Joaquin Elizalde, Emma Frederick, Ethan Grundvig, Joshua Solano -->
<!-- This page allows anyone to view participants. Managers can manipulate -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Participants</title>

    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/viewStyles.css">

</head>
<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <a href="/" class="brand">Ella Rises</a>
        <div>
            <a href="/">Home</a>
            <a href="/logout">Log Out</a>
        </div>
    </div>

    <!-- Search bar -->
    <div style="padding-top: 20px;">
    <form method="GET" action="/searchPart" style="text-align:center; margin-bottom:20px;">
        <input type="text" name="emailSearch" placeholder="Search by email..." style="padding:10px; width:60%; border:2px solid #FFD8D1; border-radius:10px;"/>
        <button 
            type="submit" 
            style="padding:10px 20px; border-radius:10px; border:2px solid #FFC4BA; background:#FFD8D1; font-weight:600;">
            Search
        </button>
    </form>
    </div>

    <!-- Error message -->
    <% if (error_message && error_message !== "") { %>
        <div class="error-box">
            <%= error_message %>
            <br><br>
            <a href="/viewPart" class="add-btn">Refresh</a>
        </div>
    <% } %>

    <!-- Add button only for managers -->
    <div class="add-btn-container">
        <% if (level === "m") { %>
        <a href="/addPart" class="add-btn">Add Participant</a>
        <% } %>
        <a href="/viewPart" class="add-btn">View All</a>
    </div>

    <div class="container">
        <h1>All Participants</h1>

        <!-- make sure there are participants in the table -->
        <% if (parts && parts.length > 0) { %>
            <div class="table-wrapper">
            <table>
                <thead>
                    <tr>
                        <% if (level === "m") { %>
                        <th>Email</th>
                        <% } %>
                        <th>First</th>
                        <th>Last</th>
                        <% if (level === "m") { %>
                        <th>Birth</th>
                        <% } %>
                        <th>Role</th>
                        <% if (level === "m") { %>
                        <th>Phone</th>
                        <% } %>
                        <th>City</th>
                        <th>State</th>
                        <% if (level === "m") { %>
                        <th>Zip</th>
                        <th>School or Employer</th>
                        <!-- Edit and delete are only seen for managers -->
                        <th>Edit</th>
                        <th>Delete</th>
                        <% } %>
                        <th>Milestones</th>
                    </tr>
                </thead>

                <tbody>
                    <!-- For each participant, display their data -->
                    <% parts.forEach(part => { 
                        // Format date safely
                        let dob = "";
                        if (part.part_dob) {
                            <!-- Format for a cleaner view -->
                            dob = part.part_dob.toISOString().split("T")[0];
                        }
                    %>
                        <tr>
                            <% if (level === "m") { %>
                            <td><%= part.part_email %></td>
                            <% } %>
                            <td><%= part.part_first_name %></td>
                            <td><%= part.part_last_name %></td>
                            <% if (level === "m") { %>
                            <td><%= dob %></td>
                            <% } %>
                            <!-- Make sure it displays full title instead of just "p" or "a" -->
                            <td><% if (part.part_role === "p") { %>Participant<% } else { %>Admin<% } %></td>
                            <% if (level === "m") { %>
                            <td><%= part.part_phone.toString().replace(/(\d{3})(\d{3})(\d{4})/, "($1) $2-$3") %></td>
                            <% } %>
                            <td><%= part.part_city %></td>
                            <td><%= part.part_state %></td>
                            <% if (level === "m") { %>
                            <td><%= part.part_zip %></td>
                            <td><%= part.part_school_or_employer %></td>
                            <% } %>

                            <!-- Edit and delete buttons for managers only -->
                            <% if (level === "m") { %>
                            <td>
                                <form action="/editPart/<%= part.part_id %>" method="get">
                                    <input class="action-btn" type="submit" value="Edit" />
                                </form>
                            </td>

                            <td>
                                <form action="/deletePart/<%= part.part_id %>" method="post" onsubmit="return confirm('Delete this participant?');">
                                    <input class="action-btn" type="submit" value="Delete" />
                                </form>
                            </td>
                            <% } %>
                            <td>
                                <!-- Anyone can view milestones for this participant -->
                                <form action="/milestones/<%= part.part_id %>" method="get">
                                    <input class="ms-action-btn" type="submit" value="Milestones" />
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
            </div>

            <!-- If there is no data in the table -->
        <% } else { %>
            <div class="empty">No users found.</div>
        <% } %>
    </div>

</body>
</html>