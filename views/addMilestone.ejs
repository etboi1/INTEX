<!-- Joaquin Elizalde, Emma Frederick, Ethan Grundvig, Joshua Solano -->
<!-- This page has a form so managers can add a Milestone to a specific user -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Milestone</title>

    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/addStyles.css">
</head>

<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <a href="/" class="brand">Ella Rises</a>
        <div>
            <a href="/">Home</a>
            <a href="/logout">Log Out</a>
        </div>
    </div>

    <!-- Leads back to the list of milestones for the user we are focused on -->
    <a href="/milestones/<%= participant.part_id %>" class="back-btn">← Back to Milestones</a>

    <div class="container">
        <h1>Add Milestone</h1>

        <!-- check for error message -->
        <% if (error_message) { %>
            <div class="error-box"><%= error_message %></div>
        <% } %>

        <!-- Post on the route tied to the specific user -->
        <form action="/milestones/<%= participant.part_id %>/add" method="POST">

            <div class="form-group">
                <label>Milestone Title</label>
                <input type="text" name="milestone_title" required>
            </div>

            <div class="form-group">
                <label>Milestone Date</label>
                <input type="date" name="milestone_date" required>
            </div>

            <button type="submit" class="submit-btn">Add Milestone</button>
        </form>
    </div>

</body>
</html>
