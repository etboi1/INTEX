<!-- Joaquin Elizalde, Emma Frederick, Ethan Grundvig, Joshua Solano -->
<!-- This page allows managers to edit donations on the global donations page -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Donation</title>

    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/addStyles.css">
</head>

<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <div class="brand">Ella Rises</div>
        <div>
            <a href="/">Home</a>
            <a href="/logout">Log Out</a>
        </div>
    </div>

    <!-- Back button to view all -->
    <a href="/viewAllDonations" class="back-btn">← Back to Donations</a>

    <div class="container">
        <h1>Edit Donation</h1>

        <!-- Check for error message -->
        <% if (error_message) { %>
            <div class="error-box"><%= error_message %></div>
        <% } %>

        <!-- Post to submit the edit and send it to the DB -->
        <form action="/editDonationGlobal/<%= donation.part_id %>/<%= donation.donation_number %>" method="POST">

            <div class="form-group">
                <label>Donation Date</label>
                <!-- Value is formatted as a working date to autofill -->
                <input type="date"
                    name="donation_date"
                    value="<%= donation.donation_date.toISOString().split('T')[0] %>"
                    required>
            </div>

            <div class="form-group">
                <label>Donation Amount</label>
                <input type="number"
                    step="0.01"
                    name="donation_amount"
                    value="<%= donation.donation_amount %>"
                    required>
            </div>

            <button type="submit" class="submit-btn">Save Changes</button>
        </form>
    </div>

</body>
</html>
