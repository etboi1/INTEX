<!-- Joaquin Elizalde, Emma Frederick, Ethan Grundvig, Joshua Solano -->
<!-- This page allows managers to  -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Location</title>

    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/addStyles.css">
</head>
<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <div class="brand">Ella Rises</div>
        <div>
            <a href="/">Home</a>
            <a href="/logout">Log Out</a>
        </div>
    </div>

    <a href="/addEvent" class="back-btn">← Back to Add Event</a>

    <div class="container">
        <h1>Add Location</h1>

        <!-- Checking for error message -->
        <% if (error_message) { %>
            <div class="error-box"><%= error_message %></div>
        <% } %>

        <!-- Post to add to database -->
        <form action="/addLocation" method="POST">

            <div class="form-group">
                <label>Location Name</label>
                <input type="text" name="location_name" required>
            </div>

            <div class="form-group">
                <label>Capacity</label>
                <input type="number" name="location_capacity" min="1" required>
            </div>

            <button type="submit" class="submit-btn">Save Location</button>
        </form>
    </div>

</body>
</html>
